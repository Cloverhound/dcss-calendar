let initialState = {
  "queues" : {
    
  }
}

const QueuesReducer = (state = initialState, action) => {
  switch (action.type) {

      

    default:
      return state
  }
}

export default QueuesReducer