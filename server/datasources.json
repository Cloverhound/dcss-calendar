{
  "db": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "url": "",
    "database": "${DB}",
    "password": "${DB_PASS}",
    "name": "db",
    "user": "${DB_USER}",
    "connector": "mssql",
    "options": {
      "encrypt": true,
      "multiSubnetFailover": "${DB_MULTI_SUB_FAILOVER}"
    }
  }
}