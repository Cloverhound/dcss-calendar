{
  "db": {
    "host": "${DB_HOST}",
    "port": 1433,
    "url": "",
    "database": "${DB}",
    "password": "${DB_PASS}",
    "name": "db",
    "user": "${DB_USER}",
    "connector": "mssql",
    "options": {
      "encrypt": true
    }
  }
}
